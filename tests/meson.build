compile_sh = find_program(
    'compile.sh',
    dirs: [meson.current_source_dir() / '../scripts'],
    required: true,
)

check_stack_usage_sh = find_program(
    'check-stack-usage.sh',
    dirs: [meson.current_source_dir() / '../scripts'],
    required: true,
)

subdir('enum-array')